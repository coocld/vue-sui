#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../.npminstall/browser-sync/2.11.1/browser-sync/bin/browser-sync.js" "$@"
  ret=$?
else 
  node  "$basedir/../.npminstall/browser-sync/2.11.1/browser-sync/bin/browser-sync.js" "$@"
  ret=$?
fi
exit $ret
